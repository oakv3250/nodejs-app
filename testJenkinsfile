pipeline{
    agent any

    stages{
      stage('Dep install'){
        agent { docker { image 'nodejs:14'
    args '-u root:root'}}
        steps{
           // sh 'sudo chown -R 995:993 "/."'
            sh 'npm install'
        }
      }
    }
}