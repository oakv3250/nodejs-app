pipeline{
    agent any

    stages{
      stage('Dep install'){
        agent { docker { image 'nodejs:14.18.0'
    args '-u root:root'}}
        steps{
            sh 'npm install'
        }
      }
    }
}